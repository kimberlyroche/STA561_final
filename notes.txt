Required for install:

	conda create -n sta561_final python=3.6 anaconda
	source activate sta561_final
	conda install tensorflow
	conda install keras

	Keras 2.0.4 installed (code written for 2.2.4); required a couple of fixes noted in shared_imports.py

PR1

subsample_no = 200
batch_size = 128
epochs = 10


PR2

subsample_no = 1000
epochs = 10

components = [...]


PR 5

